     1→     1→// TrustScore — NIN-First rebuild
     2→     2→// Stage 1–7 schema (per docs/STAGE0_AUDIT.md §4.3 / §4.2)
     3→     3→//
     4→     4→// Principle: UserAccount ≠ TrustIdentity ≠ NIN ≠ Phone (directive §29).
     5→     5→// Raw identity numbers (NIN, BVN, raw phone) are NEVER stored here.
     6→     6→// Stage 3 added the Trust Identity management layer: hashed identifiers,
     7→     7→// consent-scoped attributes, evidence records with provenance + freshness.
     8→     8→// Stage 4 adds phone (OTP) + biometric (liveness) verification sessions —
     9→     9→// contract-first MOCK providers; raw phone numbers are stored ONLY as
    10→    10→// peppered sha256 fingerprints with masked display hints.
    11→    11→// Stage 7 adds the reputation layer: flags with evidence, human-reviewed
    12→    12→// resolutions, appeals — anti-gaming by design (L2 reporter gate, one
    13→    13→// open flag per pair, 3-per-7-day quota, mandatory evidence).
    14→    14→
    15→    15→generator client {
    16→    16→  provider = "prisma-client-js"
    17→    17→}
    18→    18→
    19→    19→datasource db {
    20→    20→  provider = "sqlite"
    21→    21→  url      = env("DATABASE_URL")
    22→    22→}
    23→    23→
    24→    24→// ---------------------------------------------------------------------------
    25→    25→// Stage 1 — Accounts (authentication subjects)
    26→    26→// ---------------------------------------------------------------------------
    27→    27→
    28→    28→model UserAccount {
    29→    29→  id              String   @id @default(cuid())
    30→    30→  email           String   @unique
    31→    31→  passwordHash    String // scrypt output (never plaintext, never reversible)
    32→    32→  passwordSalt    String // per-user random salt
    33→    33→  displayName     String
    34→    34→  handle          String   @unique // reserved for future trustscore.ng/@handle Trust Link (Stage 5)
    35→    35→  status          String   @default("ACTIVE") // ACTIVE | SUSPENDED | PENDING_DELETION
    36→    36→  role            String   @default("USER") // USER | REVIEWER — reviewer role grants human-review queue access (operational grant, never self-service)
    37→    37→  acceptedTermsAt DateTime @default(now())
    38→    38→  createdAt       DateTime @default(now())
    39→    39→  updatedAt       DateTime @updatedAt
    40→    40→
    41→    41→  sessions             Session[]
    42→    42→  notifications        Notification[]
    43→    43→  trustIdentity        TrustIdentity?
    44→    44→  verificationSessions VerificationSession[]
    45→    45→  consents             Consent[]
    46→    46→  evidence             Evidence[]
    47→    47→  phoneVerifications   PhoneVerification[]
    48→    48→  livenessSessions     LivenessSession[]
    49→    49→  scoreSnapshots      TrustScoreSnapshot[]
    50→    50→  credentials          Credential[]
    51→    51→  shareTokens          ShareToken[]
    52→    52→  trustReceipts        TrustReceipt[]
    53→    53→  dsrRequests          DsrRequest[]
    54→    54→  safetyChecksMade     SafetyCheck[]     @relation("SafetyChecksMade")
    55→    55→  safetyChecksReceived SafetyCheck[]     @relation("SafetyChecksReceived")
    56→    56→  trustRequestsSent    TrustRequest[]    @relation("TrustRequestsSent")
    57→    57→  trustRequestsReceived TrustRequest[]   @relation("TrustRequestsReceived")
    58→    58→  flagsFiled          Flag[]            @relation("FlagsFiled")
    59→    59→  flagsReceived       Flag[]            @relation("FlagsReceived")
    60→    60→  flagResolutions     FlagResolution[]  @relation("FlagResolutions")
    61→    61→  flagAppeals         FlagAppeal[]      @relation("FlagAppeals")
    62→    62→  // Stage 9 — B2B developer portal
    63→    63→  apiClientsOwned     ApiClient[]       @relation("ApiClientsOwned")
    64→    64→  apiTeams            ApiTeamMember[]   @relation("ApiTeams")
    65→    65→
    66→    66→  @@index([email])
    67→    67→  @@index([status])
    68→    68→}
    69→    69→
    70→    70→// ---------------------------------------------------------------------------
    71→    71→// Stage 2 — NINAuth identity spine (contract-first, MOCK provider)
    72→    72→// Raw NIN / raw identifiers are NEVER stored. The provider returns only a
    73→    73→// masked subject reference (providerIdentityRef) + verification outcome.
    74→    74→// ---------------------------------------------------------------------------
    75→    75→
    76→    76→model TrustIdentity {
    77→    77→  id                    String    @id @default(cuid())
    78→    78→  userId                String    @unique
    79→    79→  status                String    @default("PENDING") // PENDING | VERIFIED | EXPIRED | REVOKED
    80→    80→  assuranceLevel        Int       @default(0) // 0 = none; 1 = government-verified via NINAuth; L2 phone/L3 biometric arrive Stage 4; L4 cross-signal
    81→    81→  provider              String    @default("NINAUTH_MOCK")
    82→    82→  providerMode          String    @default("MOCK") // MOCK until partner sandbox creds exist (audit §2.2)
    83→    83→  providerIdentityRef   String? // masked subject from provider — never a raw NIN
    84→    84→  establishingConsentId String? // the consent that established this identity (withdrawal semantics, Stage 3)
    85→    85→  verifiedAt            DateTime?
    86→    86→  expiresAt             DateTime? // verification freshness horizon (90 days)
    87→    87→  createdAt             DateTime  @default(now())
    88→    88→  updatedAt             DateTime  @updatedAt
    89→    89→
    90→    90→  user        UserAccount          @relation(fields: [userId], references: [id], onDelete: Cascade)
    91→    91→  identifiers IdentityIdentifier[]
    92→    92→  attributes  IdentityAttribute[]
    93→    93→  evidence    Evidence[]
    94→    94→
    95→    95→  @@index([status])
    96→    96→}
    97→    97→
    98→    98→// ---------------------------------------------------------------------------
    99→    99→// Stage 3 — Identity identifiers (HASHED — directive §29 identity_identifiers)
   100→   100→// We store only peppered sha256 fingerprints for future match lookops
   101→   101→// (Stage 6 Safety Check) — never raw NIN/phone values.
   102→   102→// ---------------------------------------------------------------------------
   103→   103→
   104→   104→model IdentityIdentifier {
   105→   105→  id              String    @id @default(cuid())
   106→   106→  trustIdentityId String
   107→   107→  type            String // NIN_FINGERPRINT | PHONE | EMAIL — signal family
   108→   108→  hash            String // sha256(pepper + provider subject / normalized value) — NEVER raw
   109→   109→  hint            String // masked display hint, e.g. "Government ID · NINAuth ref"
   110→   110→  status          String    @default("ACTIVE") // ACTIVE | REVOKED | SUPERSEDED
   111→   111→  verifiedAt      DateTime
   112→   112→  expiresAt       DateTime? // identifier freshness horizon
   113→   113→  consentId       String? // provenance
   114→   114→  createdAt       DateTime  @default(now())
   115→   115→  updatedAt       DateTime  @updatedAt
   116→   116→
   117→   117→  trustIdentity TrustIdentity @relation(fields: [trustIdentityId], references: [id], onDelete: Cascade)
   118→   118→
   119→   119→  @@unique([trustIdentityId, type])
   120→   120→  @@index([hash])
   121→   121→}
   122→   122→
   123→   123→// ---------------------------------------------------------------------------
   124→   124→// Stage 3 — Identity attributes (CONSENT-SCOPED — directive §29/§31)
   125→   125→// An attribute exists only while the consent that produced it stands.
   126→   126→// Withdrawing the consent revokes the attribute (NDPA data-subject right).
   127→   127→// ---------------------------------------------------------------------------
   128→   128→
   129→   129→model IdentityAttribute {
   130→   130→  id              String    @id @default(cuid())
   131→   131→  trustIdentityId String
   132→   132→  key             String // given_name | family_name | birth_year | state_of_origin | ...
   133→   133→  value           String // consent-scoped display value
   134→   134→  scope           String // the consent scope that produced this attribute
   135→   135→  consentId       String // provenance — withdraw the consent, revoke the attribute
   136→   136→  status          String    @default("ACTIVE") // ACTIVE | REVOKED
   137→   137→  source          String // provider that asserted the value
   138→   138→  assertedAt      DateTime // when the provider asserted it
   139→   139→  expiresAt       DateTime? // attribute freshness (follows identity horizon)
   140→   140→  createdAt       DateTime  @default(now())
   141→   141→  updatedAt       DateTime  @updatedAt
   142→   142→
   143→   143→  trustIdentity TrustIdentity @relation(fields: [trustIdentityId], references: [id], onDelete: Cascade)
   144→   144→
   145→   145→  @@unique([trustIdentityId, key])
   146→   146→  @@index([status])
   147→   147→}
   148→   148→
   149→   149→// ---------------------------------------------------------------------------
   150→   150→// Stage 3 — Evidence records (provenance + freshness — directive §29 evidence)
   151→   151→// Verified evidence with chain: which session produced it, under which consent,
   152→   152→// from which provider, with what confidence, valid until when.
   153→   153→// ---------------------------------------------------------------------------
   154→   154→
   155→   155→model Evidence {
   156→   156→  id              String    @id @default(cuid())
   157→   157→  userId          String
   158→   158→  trustIdentityId String?
   159→   159→  sessionId       String? // verification session provenance
   160→   160→  consentId       String? // consent provenance
   161→   161→  type            String // NINAUTH_ID_TOKEN | PHONE_OTP | LIVENESS (Stage 4) | ...
   162→   162→  provider        String
   163→   163→  providerMode    String
   164→   164→  summary         String // redacted human summary — no PII (directive §38)
   165→   165→  confidence      Int       @default(100) // 0–100 provider confidence
   166→   166→  status          String    @default("ACTIVE") // ACTIVE | REVOKED
   167→   167→  collectedAt     DateTime
   168→   168→  expiresAt       DateTime? // evidence freshness
   169→   169→  createdAt       DateTime  @default(now())
   170→   170→
   171→   171→  user          UserAccount    @relation(fields: [userId], references: [id], onDelete: Cascade)
   172→   172→  trustIdentity TrustIdentity? @relation(fields: [trustIdentityId], references: [id], onDelete: SetNull)
   173→   173→
   174→   174→  @@index([userId, collectedAt])
   175→   175→  @@index([trustIdentityId])
   176→   176→  @@index([status])
   177→   177→}
   178→   178→
   179→   179→model VerificationSession {
   180→   180→  id                    String    @id @default(cuid())
   181→   181→  userId                String
   182→   182→  provider              String    @default("NINAUTH_MOCK")
   183→   183→  providerMode          String    @default("MOCK")
   184→   184→  flow                  String    @default("QR") // QR | SHARE_CODE
   185→   185→  status                String    @default("AWAITING_CONSENT") // AWAITING_CONSENT | CONSENT_GRANTED | CONSENT_DENIED | COMPLETED | FAILED | EXPIRED
   186→   186→  state                 String    @unique // OAuth state / anti-CSRF (also used as nonce)
   187→   187→  codeVerifier          String // PKCE verifier — SERVER-SIDE ONLY, never sent to any client
   188→   188→  codeChallenge         String // S256(verifier)
   189→   189→  authorizationCodeHash String? // one-time code issued by the provider (mock NINAuth app)
   190→   190→  authorizationCodeExp  DateTime?
   191→   191→  scopes                String // JSON array requested/granted
   192→   192→  purpose               String
   193→   193→  policyVersion         String
   194→   194→  shareCode             String    @unique // NINAuth-style share code for the user to present
   195→   195→  authorizationUrl      String // provider authorize URL (mock endpoint shape in sandbox)
   196→   196→  errorReason           String?
   197→   197→  consentId             String?
   198→   198→  expiresAt             DateTime // session TTL (10 min, like an authorize flow)
   199→   199→  completedAt           DateTime?
   200→   200→  createdAt             DateTime  @default(now())
   201→   201→  updatedAt             DateTime  @updatedAt
   202→   202→
   203→   203→  events VerificationEvent[]
   204→   204→  user   UserAccount         @relation(fields: [userId], references: [id], onDelete: Cascade)
   205→   205→
   206→   206→  @@index([userId])
   207→   207→  @@index([status])
   208→   208→}
   209→   209→
   210→   210→model VerificationEvent {
   211→   211→  id        String   @id @default(cuid())
   212→   212→  sessionId String
   213→   213→  eventType String // SESSION_CREATED | CONSENT_SCREEN_PRESENTED | CONSENT_GRANTED | CONSENT_DENIED | CODE_ISSUED | CODE_EXCHANGED | TOKEN_VALIDATED | IDENTITY_VERIFIED | SESSION_FAILED | SESSION_EXPIRED
   214→   214→  detail    String? // redacted JSON — no PII (directive §38)
   215→   215→  createdAt DateTime @default(now())
   216→   216→
   217→   217→  session VerificationSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)
   218→   218→
   219→   219→  @@index([sessionId, createdAt])
   220→   220→}
   221→   221→
   222→   222→// Consent record per directive §31: who requested, why, what scopes were
   223→   223→// requested/granted, when, which provider, policy version.
   224→   224→model Consent {
   225→   225→  id            String    @id @default(cuid())
   226→   226→  userId        String
   227→   227→  sessionId     String?   @unique
   228→   228→  requester     String // who requested the identity access
   229→   229→  purpose       String
   230→   230→  scopes        String // JSON array granted
   231→   231→  policyVersion String
   232→   232→  grantedAt     DateTime  @default(now())
   233→   233→  withdrawnAt   DateTime?
   234→   234→  createdAt     DateTime  @default(now())
   235→   235→
   236→   236→  user UserAccount @relation(fields: [userId], references: [id], onDelete: Cascade)
   237→   237→
   238→   238→  @@index([userId])
   239→   239→}
   240→   240→
   241→   241→// ---------------------------------------------------------------------------
   242→   242→// Stage 4 — Phone verification (OTP, contract-first MOCK SMS transport).
   243→   243→// The raw phone number NEVER lands in this table: only a peppered sha256
   244→   244→// fingerprint (matching Stage 3 identifier discipline) + a masked display
   245→   245→// hint. The OTP itself is stored hashed; attempts are capped server-side.
   246→   246→// ---------------------------------------------------------------------------
   247→   247→
   248→   248→model PhoneVerification {
   249→   249→  id          String    @id @default(cuid())
   250→   250→  userId      String
   251→   251→  phoneHash   String // sha256(pepper + E.164) — NEVER raw phone
   252→   252→  phoneHint   String // masked display hint, e.g. "+234 8•• ••• ••34"
   253→   253→  status      String    @default("PENDING") // PENDING | VERIFIED | FAILED | EXPIRED
   254→   254→  otpHash     String // sha256(pepper + code) — never plaintext
   255→   255→  attempts    Int       @default(0) // wrong-OTP attempts (max 3, reset on resend)
   256→   256→  resends     Int       @default(0) // resend count (max 3)
   257→   257→  simSwapRisk String    @default("LOW") // LOW | MEDIUM | HIGH — mock network signal
   258→   258→  consentId   String // the consent that authorized this verification
   259→   259→  expiresAt   DateTime // OTP TTL (5 minutes)
   260→   260→  verifiedAt  DateTime?
   261→   261→  createdAt   DateTime  @default(now())
   262→   262→  updatedAt   DateTime  @updatedAt
   263→   263→
   264→   264→  user UserAccount @relation(fields: [userId], references: [id], onDelete: Cascade)
   265→   265→
   266→   266→  @@index([userId, createdAt])
   267→   267→  @@index([status])
   268→   268→}
   269→   269→
   270→   270→// ---------------------------------------------------------------------------
   271→   271→// Stage 4 — Biometric liveness (selfie/liveness, Smile-ID-class MOCK adapter).
   272→   272→// The provider returns a verdict contract: liveness score, face-match against
   273→   273→// the government-anchored reference, consistency flag. No selfie pixels or
   274→   274→// biometric templates are ever stored — only the redacted verdict + a
   275→   275→// fingerprint of the subject linkage.
   276→   276→// ---------------------------------------------------------------------------
   277→   277→
   278→   278→model LivenessSession {
   279→   279→  id              String    @id @default(cuid())
   280→   280→  userId          String
   281→   281→  trustIdentityId String?
   282→   282→  jobId           String    @unique // provider job reference (mock)
   283→   283→  status          String    @default("PENDING") // PENDING | PASSED | FAILED | EXPIRED
   284→   284→  result          String? // JSON verdict: passed, livenessScore, faceMatchScore, consistent, reason (redacted)
   285→   285→  confidence      Int? // provider confidence 0–100
   286→   286→  consentId       String // the consent that authorized this check
   287→   287→  expiresAt       DateTime // capture-window TTL (10 minutes)
   288→   288→  completedAt     DateTime?
   289→   289→  createdAt       DateTime  @default(now())
   290→   290→  updatedAt       DateTime  @updatedAt
   291→   291→
   292→   292→  user UserAccount @relation(fields: [userId], references: [id], onDelete: Cascade)
   293→   293→
   294→   294→  @@index([userId, createdAt])
   295→   295→  @@index([status])
   296→   296→}
   297→   297→
   298→   298→// ---------------------------------------------------------------------------
   299→   299→// Stage 8 — Trust Engine: rules-first, versioned, configurable scoring
   300→   300→// policy (audit §4.2). Every snapshot names the policy that produced it;
   301→   301→// policy changes are append-only (new version row) so old snapshots stay
   302→   302→// explainable forever. A DPIA record must cover the ACTIVE policy before
   303→   303→// automated significant decisions are allowed to ship (NDPC guidance gate).
   304→   304→// ---------------------------------------------------------------------------
   305→   305→
   306→   306→model ScoringPolicy {
   307→   307→  id            String   @id @default(cuid())
   308→   308→  version       Int      @unique // 1, 2, 3 … append-only
   309→   309→  status        String   @default("DRAFT") // DRAFT | ACTIVE | RETIRED
   310→   310→  // Rules (all clamped at write time by the service — the engine never trusts
   311→   311→  // un-validated config): component budgets, thresholds, windows, penalties.
   312→   312→  rules         String // JSON: { assuranceBase, credentials, reputation, resolution, riskPenalty, statusThresholds, freshnessTtlHours … }
   313→   313→  changeSummary String // human-readable — what changed vs the prior version
   314→   314→  createdBy     String // admin handle/id (operational grant — never self-service)
   315→   315→  activatedAt   DateTime?
   316→   316→  createdAt     DateTime @default(now())
   317→   317→
   318→   318→  @@index([status])
   319→   319→}
   320→   320→
   321→   321→// Platform-level key/value settings (DPIA gate, future engine switches).
   322→   322→// Values are JSON strings; keys are engine-owned constants.
   323→   323→model PlatformSetting {
   324→   324→  key       String   @id
   325→   325→  value     String // JSON value
   326→   326→  updatedAt DateTime @updatedAt
   327→   327→}
   328→   328→
   329→   329→// DPIA record — one per completed assessment. The ACTIVE scoring policy's
   330→   330→// DPIA must be `COMPLETED` (and not superseded) for the automated-decision
   331→   331→// gate to open. This is the NDPC-governance gate the audit demands.
   332→   332→model DpiaRecord {
   333→   333→  id            String   @id @default(cuid())
   334→   334→  policyId      String // the ScoringPolicy this assessment covers
   335→   335→  status        String   @default("IN_PROGRESS") // IN_PROGRESS | COMPLETED
   336→   336→  summary       String // scope, risks, mitigations — human-readable registry entry
   337→   337→  residualRisk  String   @default("MEDIUM") // LOW | MEDIUM | HIGH (post-mitigation)
   338→   338→  checklist     String // JSON: [{id, label, done}] — governance evidence
   339→   339→  completedAt   DateTime?
   340→   340→  createdBy     String
   341→   341→  createdAt     DateTime @default(now())
   342→   342→
   343→   343→  @@index([policyId])
   344→   344→}
   345→   345→
   346→   346→// ---------------------------------------------------------------------------
   347→   347→// Stage 5 — TrustScore snapshots (directive §35/§36 score contract).
   348→   348→// Formula: Identity Assurance + Verified Reputation + Verified Credentials +
   349→   349→// Resolution History − Confirmed Risk. Output: score, confidence, risk band,
   350→   350→// human-readable explanation (NDPA §37 automated-decision transparency) and
   351→   351→// freshness. Snapshots are immutable; a new row is written on material change
   352→   352→// or TTL expiry (24h), and only the last 20 per user are retained.
   353→   353→// Stage 8 adds the lifecycle: state (ACTIVE/STALE/FROZEN/RETIRED) + the
   354→   354→// policy that produced the snapshot (every score stays explainable) +
   355→   355→// freeze fields (appeal filed → frozen, appeal decided → recomputed).
   356→   356→// ---------------------------------------------------------------------------
   357→   357→
   358→   358→model TrustScoreSnapshot {
   359→   359→  id          String   @id @default(cuid())
   360→   360→  userId      String
   361→   361→  version     Int      @default(1)
   362→   362→  status      String // NEW | VERIFIED | ESTABLISHED | CAUTION | HIGH_RISK | REVIEW_REQUIRED
   363→   363→  score       Int // 0–100
   364→   364→  confidence  Int // 0–100
   365→   365→  riskBand    String // LOW | MEDIUM | HIGH
   366→   366→  components  String // JSON: per-component {value, max, note}
   367→   367→  explanation String // JSON array of human-readable lines (NDPA §37)
   368→   368→  inputsHash  String // canonical hash of score inputs (change detection)
   369→   369→  trigger     String // INITIAL | MATERIAL_CHANGE | PERIODIC
   370→   370→  computedAt  DateTime @default(now())
   371→   371→  expiresAt   DateTime // snapshot freshness horizon (24h)
   372→   372→  state       String   @default("ACTIVE") // Stage 8: ACTIVE | STALE | FROZEN | RETIRED
   373→   373→  policyId    String? // Stage 8: the ScoringPolicy version that produced this
   374→   374→  frozenAt    DateTime?
   375→   375→  frozenReason String? // e.g. APPEAL_PENDING
   376→   376→
   377→   377→  user UserAccount @relation(fields: [userId], references: [id], onDelete: Cascade)
   378→   378→
   379→   379→  @@index([userId, computedAt])
   380→   380→}
   381→   381→
   382→   382→// ---------------------------------------------------------------------------
   383→   383→// Stage 5 — Credentials (directive §6, wallet alignment).
   384→   384→// Platform-issued, evidence-backed assertions ("government identity verified",
   385→   385→// "phone verified", "liveness passed"). Claims are MASKED ONLY (hints/counts)
   386→   386→// — never attribute values, never raw identifiers. Credential never outlives
   387→   387→// its source evidence; user may revoke it manually at any time.
   388→   388→// ---------------------------------------------------------------------------
   389→   389→
   390→   390→model Credential {
   391→   391→  id              String    @id @default(cuid())
   392→   392→  userId          String
   393→   393→  trustIdentityId String?
   394→   394→  type            String // GOV_ID_VERIFIED | PHONE_VERIFIED | LIVENESS_VERIFIED
   395→   395→  issuer          String // "TrustScore Platform" (issuer of record)
   396→   396→  issuerMode      String    @default("MOCK") // honesty: underlying providers are MOCK
   397→   397→  claims          String // JSON, masked only (hints/counts)
   398→   398→  status          String    @default("ACTIVE") // ACTIVE | REVOKED | EXPIRED
   399→   399→  evidenceId      String? // provenance
   400→   400→  manualRevoked   Boolean   @default(false) // user-initiated revocation sticks
   401→   401→  issuedAt        DateTime  @default(now())
   402→   402→  expiresAt       DateTime?
   403→   403→  revokedAt       DateTime?
   404→   404→
   405→   405→  user UserAccount @relation(fields: [userId], references: [id], onDelete: Cascade)
   406→   406→
   407→   407→  @@unique([userId, type])
   408→   408→  @@index([status])
   409→   409→}
   410→   410→
   411→   411→// ---------------------------------------------------------------------------
   412→   412→// Stage 5 — Share tokens / Trust Link (directive §5 Trust Passport).
   413→   413→// The RAW token exists only transiently in the owner's UI (shown once at
   414→   414→// creation). At rest we keep sha256(raw) — lookup key for public views.
   415→   415→// Views are counted, bounded, expiring and revocable; every public view
   416→   416→// writes a TrustReceipt (who-checked-you log) for the owner.
   417→   417→// ---------------------------------------------------------------------------
   418→   418→
   419→   419→model ShareToken {
   420→   420→  id           String    @id @default(cuid())
   421→   421→  userId       String
   422→   422→  tokenHash    String    @unique // sha256(raw token) — never the raw token
   423→   423→  scopes       String // JSON array: PROFILE | SIGNALS | ATTRIBUTES | SCORE
   424→   424→  maxViews     Int       @default(5)
   425→   425→  views        Int       @default(0)
   426→   426→  status       String    @default("ACTIVE") // ACTIVE | REVOKED | EXPIRED
   427→   427→  expiresAt    DateTime
   428→   428→  revokedAt    DateTime?
   429→   429→  lastViewedAt DateTime?
   430→   430→  createdAt    DateTime  @default(now())
   431→   431→
   432→   432→  user UserAccount @relation(fields: [userId], references: [id], onDelete: Cascade)
   433→   433→
   434→   434→  @@index([userId, status])
   435→   435→}
   436→   436→
   437→   437→model TrustReceipt {
   438→   438→  id           String   @id @default(cuid())
   439→   439→  userId       String
   440→   440→  shareTokenId String?
   441→   441→  viewerLabel  String // e.g. "Trust link viewer" — no PII collected
   442→   442→  channel      String   @default("TRUST_LINK")
   443→   443→  cardShown    String // JSON: status/level/score shown at view time
   444→   444→  ipHash       String? // salted hash — abuse dedupe only, never displayed raw
   445→   445→  viewedAt     DateTime @default(now())
   446→   446→
   447→   447→  user UserAccount @relation(fields: [userId], references: [id], onDelete: Cascade)
   448→   448→
   449→   449→  @@index([userId, viewedAt])
   450→   450→}
   451→   451→
   452→   452→// ---------------------------------------------------------------------------
   453→   453→// Stage 5 — DSR requests (NDPA §36 data-subject rights, self-service).
   454→   454→// EXPORT: full data payload (hashes included — they ARE the stored data),
   455→   455→// downloadable for 7 days, then purged. DELETE: identity + account erased
   456→   456→// with cascade; only a redacted audit tombstone survives (legal basis).
   457→   457→// ---------------------------------------------------------------------------
   458→   458→
   459→   459→model DsrRequest {
   460→   460→  id          String    @id @default(cuid())
   461→   461→  userId      String
   462→   462→  type        String // EXPORT | DELETE
   463→   463→  status      String    @default("PENDING") // PENDING | COMPLETED | REJECTED
   464→   464→  detail      String? // redacted summary
   465→   465→  payload     String? // EXPORT only: JSON payload (7-day retention)
   466→   466→  requestedAt DateTime  @default(now())
   467→   467→  completedAt DateTime?
   468→   468→  expiresAt   DateTime? // export availability window
   469→   469→
   470→   470→  user UserAccount @relation(fields: [userId], references: [id], onDelete: Cascade)
   471→   471→
   472→   472→  @@index([userId, requestedAt])
   473→   473→}
   474→   474→
   475→   475→// ---------------------------------------------------------------------------
   476→   476→// Stage 6 — Safety Check (directive §2/§46 "Check Before You Deal").
   477→   477→// A signed-in VERIFIER runs a sanitized assessment on a subject via:
   478→   478→//   HANDLE        — the subject's @handle (requires their standing safety
   479→   479→//                   consent; anti-enumeration: "no such handle" and "checks
   480→   480→//                   disabled" are byte-identical responses)
   481→   481→//   PHONE         — consent-gated peppered-hash match against the subject's
   482→   482→//                   PHONE identifier (the raw phone is hashed and discarded;
   483→   483→//                   it is NEVER stored, logged or echoed)
   484→   484→//   TRUST_LINK/QR — an active Stage 5 share token / QR Trust Card payload
   485→   485→//                   (token scopes govern; the view is counted and receipted
   486→   486→//                   with the verifier's NAME — a named check)
   487→   487→// Every completed check writes a SafetyCheck row (the snapshot the verifier
   488→   488→// saw), a receipt + notification for the subject, and an audit event.
   489→   489→// The assessment NEVER says "this person is safe" (directive §50).
   490→   490→// ---------------------------------------------------------------------------
   491→   491→
   492→   492→model SafetyCheck {
   493→   493→  id           String    @id @default(cuid())
   494→   494→  verifierId   String
   495→   495→  subjectId    String
   496→   496→  method       String // HANDLE | PHONE | TRUST_LINK | QR
   497→   497→  assessment   String // JSON snapshot of exactly what the verifier saw (sanitized, no raw identifiers)
   498→   498→  consentId    String? // standing SAFETY_CHECK consent that authorized a HANDLE/PHONE check
   499→   499→  shareTokenId String? // governing token for TRUST_LINK/QR checks
   500→   500→  ipHash       String? // salted — abuse dedupe only
   501→   501→  createdAt    DateTime  @default(now())
   502→   502→
   503→   503→  verifier UserAccount @relation("SafetyChecksMade", fields: [verifierId], references: [id], onDelete: Cascade)
   504→   504→  subject  UserAccount @relation("SafetyChecksReceived", fields: [subjectId], references: [id], onDelete: Cascade)
   505→   505→
   506→   506→  @@index([verifierId, createdAt])
   507→   507→  @@index([subjectId, createdAt])
   508→   508→}
   509→   509→
   510→   510→// ---------------------------------------------------------------------------
   511→   511→// Stage 6 — Trust Requests (verifier-side ask). When a handle is not
   512→   512→// checkable (no standing consent), the verifier can ASK once: the subject
   513→   513→// accepts (a scoped trust link is minted and the assessment runs) or
   514→   514→// declines. Requests expire after 7 days; one pending request per pair.
   515→   515→// ---------------------------------------------------------------------------
   516→   516→
   517→   517→model TrustRequest {
   518→   518→  id           String    @id @default(cuid())
   519→   519→  verifierId   String
   520→   520→  subjectId    String
   521→   521→  status       String    @default("PENDING") // PENDING | ACCEPTED | DECLINED | EXPIRED
   522→   522→  message      String // short preset ask — no free-text PII surface
   523→   523→  shareTokenId String? // minted on ACCEPT
   524→   524→  expiresAt    DateTime
   525→   525→  respondedAt  DateTime?
   526→   526→  createdAt    DateTime  @default(now())
   527→   527→
   528→   528→  verifier UserAccount @relation("TrustRequestsSent", fields: [verifierId], references: [id], onDelete: Cascade)
   529→   529→  subject  UserAccount @relation("TrustRequestsReceived", fields: [subjectId], references: [id], onDelete: Cascade)
   530→   530→
   531→   531→  @@index([subjectId, status])
   532→   532→  @@index([verifierId, createdAt])
   533→   533→}
   534→   534→
   535→   535→model Session {
   536→   536→  id        String    @id @default(cuid())
   537→   537→  userId    String
   538→   538→  tokenHash String    @unique // sha256(token); the raw token exists only in the user's httpOnly cookie
   539→   539→  expiresAt DateTime
   540→   540→  userAgent String?
   541→   541→  ipHash    String? // salted hash of client IP — never raw IP
   542→   542→  createdAt DateTime  @default(now())
   543→   543→  revokedAt DateTime?
   544→   544→
   545→   545→  user UserAccount @relation(fields: [userId], references: [id], onDelete: Cascade)
   546→   546→
   547→   547→  @@index([userId])
   548→   548→  @@index([expiresAt])
   549→   549→}
   550→   550→
   551→   551→
   552→   552→// ---------------------------------------------------------------------------
   553→   553→// Stage 7 — Reputation: Flags with evidence (directive §29 flags/flag_evidence).
   554→   554→// A flag is a REPORT TO THE PLATFORM about a member's conduct — never a
   555→   555→// public accusation: nothing about a flag is visible to anyone except the
   556→   556→// reporter, the subject and human reviewers. Anti-gaming by design
   557→   557→// (enforced in reputation-service):
   558→   558→//   * the reporter must hold a live L2+ Trust Identity (a verified human)
   559→   559→//   * one OPEN/UNDER_REVIEW flag per reporter→subject pair (409 duplicate)
   560→   560→//   * at most 3 flags per rolling 7-day window per reporter (429 window)
   561→   561→//   * at least one evidence item is mandatory at submission (422 zod)
   562→   562→//   * reporter identity is MASKED to the subject (retaliation shield);
   563→   563→//     the full record remains available via NDPA DSR export
   564→   564→// ---------------------------------------------------------------------------
   565→   565→
   566→   566→model Flag {
   567→   567→  id                 String    @id @default(cuid())
   568→   568→  reporterId         String
   569→   569→  subjectId          String
   570→   570→  category           String // FRAUD | IMPERSONATION | NON_PAYMENT | SCAM | HARASSMENT | OTHER
   571→   571→  description        String // the reporter's claim — shown to subject + reviewer
   572→   572→  status             String    @default("OPEN") // OPEN | UNDER_REVIEW | RESOLVED_CONFIRMED | RESOLVED_UNFOUNDED | RESOLVED_DISMISSED | WITHDRAWN
   573→   573→  evidenceCount      Int       @default(0)
   574→   574→  subjectRespondedAt DateTime?
   575→   575→  createdAt          DateTime  @default(now())
   576→   576→  updatedAt          DateTime  @updatedAt
   577→   577→
   578→   578→  reporter   UserAccount     @relation("FlagsFiled", fields: [reporterId], references: [id], onDelete: Cascade)
   579→   579→  subject    UserAccount     @relation("FlagsReceived", fields: [subjectId], references: [id], onDelete: Cascade)
   580→   580→  evidence   FlagEvidence[]
   581→   581→  resolution FlagResolution?
   582→   582→  appeal     FlagAppeal?
   583→   583→
   584→   584→  @@index([subjectId, status])
   585→   585→  @@index([reporterId, createdAt])
   586→   586→}
   587→   587→
   588→   588→// Evidence items attached by either party. Content is their own statement or
   589→   589→// reference (text/link) — user data: shown to the two parties + reviewers,
   590→   590→// included in the DSR export, NEVER in audit metadata (directive §38).
   591→   591→
   592→   592→model FlagEvidence {
   593→   593→  id            String   @id @default(cuid())
   594→   594→  flagId        String
   595→   595→  submittedById String
   596→   596→  role          String // REPORTER | SUBJECT — which side submitted it
   597→   597→  kind          String // TEXT | LINK
   598→   598→  content       String
   599→   599→  createdAt     DateTime @default(now())
   600→   600→
   601→   601→  flag Flag @relation(fields: [flagId], references: [id], onDelete: Cascade)
   602→   602→
   603→   603→  @@index([flagId])
   604→   604→}
   605→   605→
   606→   606→// Human-reviewed resolution (directive §33 — no automated significant
   607→   607→// decisions in Stage 7). A REVIEWER decides CONFIRMED / UNFOUNDED / DISMISSED
   608→   608→// with a published rationale. CONFIRMED sets fraudSignal=true and feeds the
   609→   609→// score engine's Confirmed Risk component (−25 per confirmed flag, cap −50).
   610→   610→
   611→   611→model FlagResolution {
   612→   612→  id          String   @id @default(cuid())
   613→   613→  flagId      String   @unique
   614→   614→  reviewerId  String
   615→   615→  outcome     String // CONFIRMED | UNFOUNDED | DISMISSED
   616→   616→  rationale   String // published to subject + reporter
   617→   617→  fraudSignal Boolean  @default(false) // true when outcome = CONFIRMED
   618→   618→  decidedAt   DateTime @default(now())
   619→   619→
   620→   620→  reviewer UserAccount @relation("FlagResolutions", fields: [reviewerId], references: [id], onDelete: Cascade)
   621→   621→  flag     Flag        @relation(fields: [flagId], references: [id], onDelete: Cascade)
   622→   622→
   623→   623→  @@index([reviewerId])
   624→   624→}
   625→   625→
   626→   626→// Subject appeal of a CONFIRMED resolution — one per flag, 14-day window
   627→   627→// from the decision. UPHELD keeps the outcome; OVERTURNED flips the flag to
   628→   628→// RESOLVED_UNFOUNDED and restores the score (NDPA §37 redress path).
   629→   629→
   630→   630→model FlagAppeal {
   631→   631→  id           String    @id @default(cuid())
   632→   632→  flagId       String    @unique
   633→   633→  appellantId  String // the flag subject
   634→   634→  reason       String
   635→   635→  status       String    @default("PENDING") // PENDING | UPHELD | OVERTURNED
   636→   636→  reviewerId   String?
   637→   637→  decisionNote String?
   638→   638→  decidedAt    DateTime?
   639→   639→  createdAt    DateTime  @default(now())
   640→   640→
   641→   641→  appellant UserAccount @relation("FlagAppeals", fields: [appellantId], references: [id], onDelete: Cascade)
   642→   642→  flag      Flag        @relation(fields: [flagId], references: [id], onDelete: Cascade)
   643→   643→
   644→   644→  @@index([status])
   645→   645→}
   646→   646→
   647→   647→// ---------------------------------------------------------------------------
   648→   648→// Stage 1 — Audit + notification plumbing (Security Center arrives Stage 5)
   649→   649→// ---------------------------------------------------------------------------
   650→   650→
   651→   651→model AuditEvent {
   652→   652→  id          String   @id @default(cuid())
   653→   653→  actorType   String // USER | SYSTEM | ANONYMOUS
   654→   654→  actorId     String?
   655→   655→  action      String // AUTH_REGISTER | AUTH_LOGIN | AUTH_LOGIN_FAILED | AUTH_LOGOUT | ...
   656→   656→  subjectType String?
   657→   657→  subjectId   String?
   658→   658→  requestId   String?
   659→   659→  metadata    String? // JSON, redacted — no PII, no raw identifiers (directive §38)
   660→   660→  createdAt   DateTime @default(now())
   661→   661→
   662→   662→  @@index([action])
   663→   663→  @@index([actorId])
   664→   664→  @@index([createdAt])
   665→   665→}
   666→   666→
   667→   667→model Notification {
   668→   668→  id        String    @id @default(cuid())
   669→   669→  userId    String
   670→   670→  type      String // SECURITY | VERIFICATION | SYSTEM
   671→   671→  title     String
   672→   672→  body      String
   673→   673→  readAt    DateTime?
   674→   674→  createdAt DateTime  @default(now())
   675→   675→
   676→   676→  user UserAccount @relation(fields: [userId], references: [id], onDelete: Cascade)
   677→   677→
   678→   678→  @@index([userId, readAt])
   679→   679→}
   680→   680→
   681→   681→// ---------------------------------------------------------------------------
   682→   682→// Stage 9 — B2B Platform (audit §4.2: developer portal, API keys, webhooks,
   683→   683→// usage, team/RBAC, Trust Decision API POST /v1/trust/check).
   684→   684→//
   685→   685→// A member registers "API clients" (business apps). Each client has a
   686→   686→// SANDBOX or LIVE environment (LIVE is an HONEST label — underlying identity
   687→   687→// providers stay contract-first MOCK until partner credentials exist), a
   688→   688→// mock-billing plan with REAL daily check quotas, API keys (raw key exists
   689→   689→// only in the minting response; sha256 at rest — session-token discipline),
   690→   690→// an optional webhook endpoint (HMAC-SHA256-signed deliveries, bounded
   691→   691→// retries) and a team (OWNER / DEVELOPER / VIEWER — enforced server-side).
   692→   692→//
   693→   693→// Compliance carried over: the Trust Decision API is a CONSENT-GATED
   694→   694→// assessment surface — subjects control API checks through the SAME standing
   695→   695→// SAFETY_CHECK consent as member checks, every completed check is receipted
   696→   696→// to the subject with the business's name, and responses are band-level
   697→   697→// with the locked language (never "this person is safe" — directive §50).
   698→   698→// ---------------------------------------------------------------------------
   699→   699→
   700→   700→model ApiClient {
   701→   701→  id            String    @id @default(cuid())
   702→   702→  ownerId       String
   703→   703→  name          String // the business's public label (shown in subject receipts)
   704→   704→  environment   String    @default("SANDBOX") // SANDBOX | LIVE (LIVE = typed-confirmation honesty label)
   705→   705→  status        String    @default("ACTIVE") // ACTIVE | SUSPENDED
   706→   706→  plan          String    @default("FREE") // FREE | STARTER — mock billing (no processor connected)
   707→   707→  webhookUrl    String? // https://… (SANDBOX also allows http://localhost for self-tests)
   708→   708→  webhookSecret String? // HMAC shared secret (whsec_…) — must stay readable to SIGN deliveries (industry-standard shared secret); shown once, rotatable. Not PII.
   709→   709→  liveEnabledAt DateTime?
   710→   710→  createdAt     DateTime  @default(now())
   711→   711→  updatedAt     DateTime  @updatedAt
   712→   712→
   713→   713→  owner      UserAccount       @relation("ApiClientsOwned", fields: [ownerId], references: [id], onDelete: Cascade)
   714→   714→  team       ApiTeamMember[]
   715→   715→  keys       ApiKey[]
   716→   716→  decisions  TrustDecision[]
   717→   717→  usageDays  ApiUsageDay[]
   718→   718→  deliveries WebhookDelivery[]
   719→   719→
   720→   720→  @@index([ownerId])
   721→   721→  @@index([status])
   722→   722→}
   723→   723→
   724→   724→// API key — the raw key (tsk_<env>_<hex>) is returned EXACTLY ONCE at mint
   725→   725→// time; at rest only sha256(raw) + a short identification prefix.
   726→   726→model ApiKey {
   727→   727→  id            String    @id @default(cuid())
   728→   728→  clientId      String
   729→   729→  name          String // developer-chosen label
   730→   730→  keyPrefix     String // first ~14 chars of the raw key — identification only
   731→   731→  keyHash       String    @unique // sha256(raw) — never the raw key
   732→   732→  scope         String    @default("TRUST_CHECK") // TRUST_CHECK (single scope in v1)
   733→   733→  status        String    @default("ACTIVE") // ACTIVE | REVOKED
   734→   734→  totalRequests Int       @default(0)
   735→   735→  lastUsedAt    DateTime?
   736→   736→  expiresAt     DateTime? // mint + 365d (informational)
   737→   737→  revokedAt     DateTime?
   738→   738→  createdAt     DateTime  @default(now())
   739→   739→
   740→   740→  client ApiClient       @relation(fields: [clientId], references: [id], onDelete: Cascade)
   741→   741→  usage  ApiUsageEvent[]
   742→   742→  decisions TrustDecision[]
   743→   743→
   744→   744→  @@index([clientId, status])
   745→   745→}
   746→   746→
   747→   747→// Team membership per client — RBAC enforced server-side on every portal
   748→   748→// route. OWNER (creator) manages team + LIVE; DEVELOPER manages keys +
   749→   749→// webhook; VIEWER is read-only. Membership is also the visibility grant:
   750→   750→// a client appears in a member's portal ONLY through this table.
   751→   751→model ApiTeamMember {
   752→   752→  id        String   @id @default(cuid())
   753→   753→  clientId  String
   754→   754→  userId    String
   755→   755→  role      String // OWNER | DEVELOPER | VIEWER
   756→   756→  addedById String
   757→   757→  createdAt DateTime @default(now())
   758→   758→
   759→   759→  client ApiClient  @relation(fields: [clientId], references: [id], onDelete: Cascade)
   760→   760→  user   UserAccount @relation("ApiTeams", fields: [userId], references: [id], onDelete: Cascade)
   761→   761→
   762→   762→  @@unique([clientId, userId])
   763→   763→}
   764→   764→
   765→   765→// One row per Trust Decision API call. Records exactly what the business
   766→   766→// was shown (the assessment snapshot) with masked input hints — never raw
   767→   767→// phone digits; a handle echo is the business's own request data. Anti-
   768→   768→// enumeration is preserved in the RESPONSE (identical UNAVAILABLE shape);
   769→   769→// this row is the business's own audit trail.
   770→   770→model TrustDecision {
   771→   771→  id           String   @id @default(cuid())
   772→   772→  clientId     String
   773→   773→  keyId        String
   774→   774→  subjectId    String? // null when there is no consented match (probe record only)
   775→   775→  method       String // HANDLE | PHONE | TRUST_LINK | QR
   776→   776→  inputHint    String // masked: "@handle" | "verified phone (hashed)" | "trust link"
   777→   777→  outcome      String // OK | UNAVAILABLE | DEAD_LINK
   778→   778→  assessment   String? // JSON — exactly what was returned (band-level, no score number)
   779→   779→  consentId    String? // standing SAFETY_CHECK consent that authorized a HANDLE/PHONE check
   780→   780→  shareTokenId String? // governing token for TRUST_LINK/QR checks
   781→   781→  requestId    String
   782→   782→  createdAt    DateTime @default(now())
   783→   783→
   784→   784→  client ApiClient @relation(fields: [clientId], references: [id], onDelete: Cascade)
   785→   785→  key    ApiKey    @relation(fields: [keyId], references: [id], onDelete: Cascade)
   786→   786→
   787→   787→  @@index([clientId, createdAt])
   788→   788→}
   789→   789→
   790→   790→// Daily usage rollup per key (90-day retention) — feeds quota enforcement
   791→   791→// and the portal usage charts. Counts are non-PII.
   792→   792→model ApiUsageDay {
   793→   793→  id        String   @id @default(cuid())
   794→   794→  keyId     String
   795→   795→  clientId  String
   796→   796→  day       String // "YYYY-MM-DD" (server date)
   797→   797→  checks    Int      @default(0)
   798→   798→  errors    Int      @default(0)
   799→   799→  updatedAt DateTime @updatedAt
   800→   800→
   801→   801→  client ApiClient @relation(fields: [clientId], references: [id], onDelete: Cascade)
   802→   802→
   803→   803→  @@unique([keyId, day])
   804→   804→}
   805→   805→
   806→   806→// Per-request usage events — bounded (last 100 per key, pruned on write).
   807→   807→model ApiUsageEvent {
   808→   808→  id         String   @id @default(cuid())
   809→   809→  keyId      String
   810→   810→  route      String
   811→   811→  statusCode Int
   812→   812→  outcome    String // OK | UNAVAILABLE | DEAD_LINK | REJECTED
   813→   813→  latencyMs  Int
   814→   814→  createdAt  DateTime @default(now())
   815→   815→
   816→   816→  key ApiKey @relation(fields: [keyId], references: [id], onDelete: Cascade)
   817→   817→
   818→   818→  @@index([keyId, createdAt])
   819→   819→}
   820→   820→
   821→   821→// Webhook delivery — the exact payload + HMAC signature we sent, the
   822→   822→// endpoint's response code/snippet, and the bounded retry schedule
   823→   823→// (attempts at +0s, +60s, +5m, +25m, +2h; retries are processed lazily on
   824→   824→// portal reads and subsequent API calls). Retention: last 50 per client.
   825→   825→model WebhookDelivery {
   826→   826→  id             String    @id @default(cuid())
   827→   827→  clientId       String
   828→   828→  event          String // TRUST_CHECK_COMPLETED | WEBHOOK_TEST
   829→   829→  payload        String // JSON — exactly what was sent (band-level, masked inputs)
   830→   830→  status         String    @default("PENDING") // PENDING | DELIVERED | RETRYING | EXHAUSTED
   831→   831→  attempts       Int       @default(0)
   832→   832→  statusCode     Int?
   833→   833→  responseSnippet String? // first 200 chars of the endpoint response
   834→   834→  signature      String? // last computed "t=…,v1=…" header value (verification aid)
   835→   835→  durationMs     Int?
   836→   836→  lastAttemptAt  DateTime?
   837→   837→  nextRetryAt    DateTime?
   838→   838→  deliveredAt    DateTime?
   839→   839→  createdAt      DateTime  @default(now())
   840→   840→
   841→   841→  client ApiClient @relation(fields: [clientId], references: [id], onDelete: Cascade)
   842→   842→
   843→   843→  @@index([clientId, createdAt])
   844→   844→  @@index([status, nextRetryAt])
   845→   845→}
   846→   846→