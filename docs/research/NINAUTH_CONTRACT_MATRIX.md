# NINAuth Contract Matrix (Batch 0, Task 0-2a)

Row-by-row reconciliation of **official NINAuth documentation** against **TrustScore's MOCK implementation**. Status legend: **DOCUMENTED** = present in fetched official sources · **PARTIAL** = described but incomplete (missing URL/fields/format) · **UNCONFIRMED** = not in any fetched official source.

Primary sources: **DG** = https://ninauth.nimc.gov.ng/developers-guide (sectioned guide) · **PM** = https://documenter.getpostman.com/view/42869517/2sAYdmk815 ("NINAuth API Documentation [v1.0.1]") · **SITE** = other ninauth.nimc.gov.ng pages (per-row URL) · **APP** = app.ninauth.nimc.gov.ng dashboard JS bundle. All fetched 2026-09-17. See NINAUTH_RESEARCH.md for prose detail.

| Capability | Official documentation (URL) | Status | Actual endpoint | Request fields | Response fields | Scopes | Consent | Security requirements | TrustScore implementation (our repo, MOCK posture today: internal mock scopes identity.basic/identity.nin_status/profile.name/profile.demographics marked mock-contract-only; LOOPBACK provider-simulator; PKCE server-side state; scope-guard pipeline granted-scopes→allowed-attributes→normalization→minimal-storage; "Continue with NINAuth" passwordless start/approve/callback; LIVE gated behind credentials) | Unknowns |
|---|---|---|---|---|---|---|---|---|---|---|
| Authorization start (OAuth) | DG "Single sign-on" — ninauth.nimc.gov.ng/developers-guide | **PARTIAL** | `/oauth/authorize` (relative only; base URL never published) | `client_id`, `app_id`, `redirect_uri`, `response_type=code`, `scope` (requested), `state`, `code_challenge`, `code_challenge_method=S256` | n/a (redirect back to registered callback with `code` + `state`) | "requested scope" shown at approval; **no scope strings published** | NINAuth hosts login, QR/code entry, app approval, consent polling; user sees registered client identity, name, logo, requested scope | Fresh PKCE verifier + state per request; redirect to registered callback only | `POST /api/v1/auth/ninauth/start` (ours): creates session, `generateState()` (24-byte hex), PKCE S256 challenge, builds authorizationUrl; rate 10/min; audit AUTH_NINAUTH_START | Full authorize URL; meaning of `app_id` vs `client_id`; scope string vocabulary; max scope set |
| Token exchange | DG "Single sign-on" | **PARTIAL** | `/oauth/token` (relative only) | `code` + original `code_verifier` (client auth at this endpoint unspecified) | "the returned tokens" — **schema, fields, TTLs all unpublished** | Implied token scoping; no strings | (covered by authorize-time consent) | Verifier must hash back to original challenge; code alone insufficient | Callback exchanges one-time code (hashed at rest, 60s TTL) with server-held verifier via provider transport; mock ID-token mint | Response schema; auth method (header vs body); token types; error format |
| PKCE | DG "Single sign-on" | **DOCUMENTED** | (mechanism, applies to /oauth/authorize + /oauth/token) | `code_challenge = SHA256(code_verifier)`, method `S256`; verifier kept private (guide says "browser storage" — SPA wording) | n/a | n/a | n/a | Stolen authorization code cannot be completed without private verifier | Real PKCE S256 (`generatePkce` in providers/ninauth.ts); verifier server-side (confidential-client hardening, never leaves backend) | Whether NINAuth also accepts `plain` (assume no); verifier entropy requirements |
| `state` | DG "Single sign-on" | **DOCUMENTED** | /oauth/authorize param; returned on callback | fresh per request | returned with `code` | n/a | n/a | Anti-CSRF (implied by flow) | Exact-match state check at callback; replayed callbacks rejected; `state_mismatch` → fail-closed + AUTH_NINAUTH_FAILED audit | Format/length constraints; TTL expectations |
| `nonce` (OIDC) | — (none found) | **UNCONFIRMED** | — | — | — | — | — | — | We generate nonce = session.state and validate it inside `validateIdToken` (OIDC-correct hardening; honestly ours, not NINAuth-documented) | Whether NINAuth supports/issues nonce at all |
| ID token / signed assertion | DG "Authenticate with NINAuth" | **PARTIAL** | Delivered to "your registered endpoint" (callback) | n/a | Prose list: claims the user approved, verification status, scoped attributes, **consent reference**, issued-at timestamp | "scoped attributes" per approved claims | Consent reference returned in-assertion; approval logged to user consent history | "Validate the signature before establishing a session" | `validateIdToken`: signature (HMAC in MOCK/LOOPBACK; JWKS-shaped trust config), issuer, audience, expiry, nonce; MOCK issuer `https://ninauth.nimc.gov.ng/mock`, LOOPBACK issuer `https://provider-simulator.loopback/ninauth`; ID-token TTL 300s | Signature algorithm (asymmetric keys?); issuer/audience values; JWKS URL; claim names |
| UserInfo | DG "Single sign-on" | **PARTIAL** | `/oauth/userinfo` (relative only) | unspecified | unspecified ("direct profile lookup") | unspecified | unspecified | unspecified (post-token) | Not implemented (not needed by our flows today) | URL, auth method, response shape, scope mapping |
| Refresh token | — (none found) | **UNCONFIRMED** | — | — | — | — | — | — | Not implemented; sessions are our own cookies | Existence, grant shape, rotation, revocation |
| Scopes / granted scopes | DG "Authenticate with NINAuth" + "Single sign-on" | **PARTIAL** | n/a (concept in authorize/assertion) | `scope` param on authorize | assertion carries "the claims the user approved" + "scoped attributes" | **No scope names published anywhere**; requested→user-approves→granted semantics documented; verification-API analog = `dataRequested[]` field-level | Requested scope displayed pre-approval; only approved claims returned | n/a | Mock SCOPE_CATALOG (marked mock-contract-only): `identity.basic` (core), `identity.nin_status` (core), `profile.name` → given_name/family_name, `profile.demographics` → birth_year/state_of_origin (+`identity.phone_status` Stage-4); granted-scopes-authoritative pipeline | Real scope strings; core vs optional split; per-scope claim mapping |
| Field-level data catalog (PII fields) | PM — documenter.getpostman.com/view/42869517/2sAYdmk815 | **DOCUMENTED** | `GET /api/v1/integration/enterprise/pii-fields` (headers client-id/client-secret) | — | 50+ paths: `nin`, `biographicData.*` (names, DOB, gender, birth/origin/residence, nextOfKin×10, religion, height, profession, education/employment status, deathVerification, enrolledAt…), `biometricData`, `biometricData.image`, `contactData.email/phone1/phone2`, `credentialData` | n/a (this is the verification-API consent vocabulary, not OAuth scopes) | Fields requested via `dataRequested[]`; user approves per request in app | client-id/client-secret headers | Our attribute keys (given_name, family_name, birth_year, state_of_origin) are a deliberate minimal subset; scope-guard drops anything not granted | Which fields TrustScore may request as an Enterprise; whether `nin` is ever grantable to non-restricted partners (assume NO) |
| Dynamic QR flow (online) | PM + DG "What is the NINAuth flow?"/use cases | **DOCUMENTED** | `POST /api/v1/integration/enterprise/online/session/create`; `GET …/online/session/regenerate/{Session_ID}`; `POST …/online/session/verification-report` | create: `enterpriseId`, `dataRequested[]`, `requestReason`; report: `enterpriseId`, `sessionId`, `provisionalId`, `provisionalIdName`, `requestId` | create 201: `expires`, `qrCode` (base64 JWT), `qrCodeBase64`, `requestId`, `sessionId`; report: only requested fields (e.g. `biographicData:{firstName,lastName}`) + `requestId` | field-level via dataRequested | User scans, sees enterprise details/reason/fields, approves in app in real time; 2-minute expiry; QR = HS256 JWT `{exp, requestCode, type:"dynamic"}` | client-id/client-secret headers; Enterprise ID in path | Not implemented (individual-posture product; enterprise QR generation is a future Verification-Partner-track feature) | Enterprise onboarding for us; QR signing key handling; polling vs webhook for session completion |
| Static QR flow | PM | **DOCUMENTED** | `POST /api/v1/integration/enterprise/online/static/generate` | `enterpriseId`, `dataRequested[]`, `requestReason`, `requestType` (e.g. "Account opening") | 201: `dataRequested[]`, `qrCode`, `qrCodeBase64` | field-level | Reusable, not time-bound; request type shown when scanned | client-id/client-secret headers | Not implemented | Static-code revocation/rotation; scan-count limits |
| Share Code (NINAuth) | PM + DG "What is the NINAuth flow?" + SITE /get-started | **DOCUMENTED** | `POST /api/v1/integration/enterprise/sharecode/verify/{Enterprise_ID}` | `shareCode` (6-char alphanumeric, **uppercase**), `requestReason` (from fixed catalog) | `biographicData`, `biometricData[]` (face photo base64), `contactData`, `requestId` | n/a (field-level consent set at code generation in app) | User-generated in NINAuth app; time-limited; one-time; enterprise validates via dashboard or API; recorded for traceability | client-id/client-secret headers; Enterprise ID in path | Not implemented as a NINAuth consumer. **Deliberately distinct** from our TrustScore Share Token (`ts_`+base64url 24B, hashed at rest, revocable, receipted) — disjoint names/lifetimes/trust basis | Exact TTL; retry/lockout rules; error codes on invalid/expired code |
| Business sign-in / RC number | — (none found) | **UNCONFIRMED** | — | — | — | — | — | — | Not implemented; no such flow assumed | Whether any business-identity authentication exists (only CAC docs + directors' NINs at enterprise *registration* are documented) |
| Enterprise registration & onboarding | PM (collection description + endpoints) + SITE /enterprises | **DOCUMENTED** | `GET /api/v1/enterprise/registration-data`; `POST /api/v1/enterprise/register` (multipart) | formdata: `data` JSON (name, email, phone_number, address, website, industry, company_type, directors[] incl. NINs) + files: `logo`, `cac_memart`, `cac_certificate`, `data_protection_policy`, `ndpa_certificate` | 200: `data:{id (Enterprise ID), name, email, client_id, created_at, status:"awaiting_approval"}`; registration-data: company_types + industries lists (use `text` values verbatim) | n/a | Director NINs + CAC docs; NDPA certificate required | client-id/client-secret headers; Enterprise ID distinct from client-id; NIMC onboarding → dashboard credentials → static IP whitelist → callback URL → sandbox → live | Not applicable (NIMC/partner-side); our .env models the resulting knobs | Approval SLA beyond "awaiting_approval"; dashboard Enterprise-ID creation flow parity |
| Raw-NIN verification | PM + SITE /get-started | **DOCUMENTED** (restricted, legacy) | `POST /api/v1/integration/enterprise/raw-nin/verify/{Enterprise_ID}` | `nin`, `requestReason` | `biographicData`, `biometricData[]`, `contactData` | n/a | Selfie-free; restricted to financial institutions + government agencies; "being phased out by NIMC" | client-id/client-secret headers | **Never implement.** scope-guard RAW_IDENTIFIER tripwire blocks any `nin`-keyed payload (RAW_IDENTIFIER_BLOCKED, nothing stored, SYSTEM audit) | N/A — we treat as forbidden by policy |
| In-person verification (NIN + selfie) | PM | **DOCUMENTED** | `POST /api/v1/integration/enterprise/in-person/create/{Enterprise_ID}`; complete via shared `…/in-person/session/verification-report` | create: `nin`, `requestReason`, `selfieImage.image` (base64); report: `enterpriseId`, `requestId`, `sessionId`, `provisionalId`, `provisionalIdName` | create: `requestId`, `sessionId`; report: `biographicData` + `biometricData[]` + `contactData` + `requestId` | n/a | **Selfie image serves as consent**; for users without devices | client-id/client-secret headers | Not implemented (requires NIN collection — conflicts with our never-store-NIN stance) | Liveness requirements on the selfie; provisionalId semantics |
| Face match with NIN (gov-only) | PM | **DOCUMENTED** | `POST /api/v1/integration/enterprise/in-person/create-with-face-auth/{Enterprise_ID}` | `nin`, `requestReason`, `liveliness_images[4]`, `selfieImage` | `requestId`, `sessionId`, `faceAuthStatus:"approved"`, `statusReason:"Match"` | n/a | Restricted: government security agencies only | client-id/client-secret headers | Not implemented (gov-restricted) | Other faceAuthStatus values; threshold details |
| Phone-number verification (gov-only) | PM | **DOCUMENTED** | `POST /api/v1/integration/enterprise/phonenumbers/verify/{Enterprise_ID}` | `phone`, `requestReason` | biographic (+ residence) data, `consentID`, `contactData`, `requestID` | n/a | Restricted: government security agencies only | client-id/client-secret headers | Not implemented (gov-restricted) | — |
| Demography search (gov-only) | PM | **DOCUMENTED** | `POST /api/v1/integration/enterprise/demography-data/verify/{Enterprise_ID}` | `firstName`, `lastName`, `dateOfBirth` (DD-MM-YYYY), `gender`, `requestReason` | close-match biographic data, `biometricData[]`, `consentID`, `contactData`, `requestID` | n/a | Restricted: government security agencies only; returns close matches | client-id/client-secret headers | Not implemented (gov-restricted; fuzzy-match privacy risk) | Match-threshold semantics |
| Request-reasons catalog | PM | **DOCUMENTED** | `GET /api/v1/integration/enterprise/request-reasons` | — | 39 `{key,label}` pairs (taxationEnrollment … blacklistSocial; see research §5.3) | n/a | Purpose binding: reason travels with every verification call | client-id/client-secret headers | Our purposes (`SELF_IDENTITY_VERIFICATION`, `NINAUTH_AUTHENTICATION`) are internal-only; LIVE mapping to catalog keys TBD | Which reasons an Enterprise (vs gov) may cite |
| Health check | PM | **DOCUMENTED** | `GET /health` | — | `{status, success, message, data:{api_gateway_status:"Live"}}` | n/a | n/a | none | Provider-simulator exposes `/health` on :3032; provider-posture probe uses it | Component-level health (status page names: QRcode Auth Service, Sharecode Verification, Biometric Verification, ID Check Services, Email/OTP-SMS Authentication) |
| Client credentials & secret handling | PM (collection description) + APP dashboard strings | **DOCUMENTED** | headers on all protected endpoints; dashboard `regenerate-client-secret-key` | `client-id`, `client-secret` headers | dashboard shows API-keys history (active/revoked) | n/a | n/a | Backend/dashboard-issued; regeneration invalidates old secret; secret never in frontend | `NINAUTH_CLIENT_SECRET` env (backend-only, never sent to client); MOCK client `trustscore_sandbox`; LOOPBACK_SIGNING_SECRET for simulator | Live issuance flow; secret rotation cadence requirements |
| IP whitelisting | PM (collection description) + APP dashboard | **DOCUMENTED** | dashboard `/company/settings/whitelisted-ips` (+ agent variant) | dashboard UI | — | n/a | n/a | **Mandatory in production, cannot be bypassed**; static/persistent IPs only; changes logged & auditable | Egress-IP planning noted for LIVE gate; not yet enforced in MOCK | Our production egress IP(s) registration |
| Crypto keys (RSA) for verification requests | APP dashboard strings (`/company/crypto-keys/active`) | **PARTIAL** | dashboard-managed (no public API in collection) | — | active public/private key status | n/a | n/a | "generate, copy or download the cryptographic keys used to encrypt and decrypt NIN verification requests"; private key never shared | LOOPBACK simulator signs with its own secret; JWKS-shaped trust config ready for real keys | Algorithm details; whether keys sign assertions or only encrypt requests; rotation |
| Webhook / verification events | APP dashboard string + PM collection description ("configure your callback URL to receive verification responses") | **PARTIAL** | partner-configurable webhook URL (dashboard `/company/settings/webhook-url`); no public event API documented | — | — | — | — | Webhook URL registered via dashboard | webhook-worker mini-service (:3031) exists as the architectural slot; LIVE adapter interface designed, semantics deferred | Event names, payload schema, signature scheme (HMAC? JWS?), retry policy |
| Consent screens | DG "Authenticate with NINAuth" + PM folder descriptions | **PARTIAL** | n/a (NINAuth-hosted) | n/a | n/a | requested scope / dataRequested + reason (+ request type for static QR) displayed | NINAuth displays registered client identity, name, logo, requested scope; user approves/rejects in real time; approval → consent history + consent reference | Registered client identity enforced | `consentScreenFor` with `NINAUTH_AUTHENTICATION` purpose override (auth flow); mock NINAuth modal shows requester identity + granular scopes (core locked, profile.name opt-in); audit trail | Exact screen spec/assets; scope-vs-field presentation; consent-reference format |
| SDK (web) | — (none found; only ssologin.nimc.gov.ng search hint) | **UNCONFIRMED** | — | — | — | — | — | — | None used; plain OAuth redirect shape in our contract | Existence of any official web SDK/JS library |
| SDK (mobile / Expo / React Native) | — (none found) | **UNCONFIRMED** | — | — | — | — | — | — | None; our integration is web-only today | Existence; app-link/deep-link guidance for native clients |
| Error codes | — (none found) | **UNCONFIRMED** | — | — | documented envelope only: `{status, success, message, data}` (200/201 examples); `awaiting_approval`; `faceAuthStatus`/`statusReason` | n/a | n/a | — | Our own codes (SCOPE_INVALID, RAW_IDENTIFIER_BLOCKED, state_mismatch, code replay, rate-limit) — clearly ours, never presented as NINAuth's | Full LIVE error table (codes, messages, retry semantics) — must be derived from sandbox |
| Rate limits | — (none found) | **UNCONFIRMED** | — | — | — | — | — | — | start 10/min; approve/callback tighter (ours) | NINAuth's actual limits |
| SSO (org session federation) | DG "Single sign-on" | **PARTIAL** | same authorize flow from each registered service | same as authorization start | session resolution (no fresh auth) when org session exists | n/a | One consent-history entry per full authentication | Never crosses org boundaries; performs no new verification | Not implemented (single-service today) | Session lifetime; logout/propagation semantics |
| Passwordless sign-in ("Continue with NINAuth") | DG "Authenticate with NINAuth" | **DOCUMENTED** | via /oauth/authorize flow (see above) | see authorization start | signed assertion (see ID token row) | requested scope | app approval with code or QR scan; no credentials stored | No password DB; biometric-enrolled identity; validate signature before session | Full mock implementation: start/approve/callback routes, one-time `nac_` codes (60s, hashed), LINK/LOGIN/REGISTERED resolution, session cookie + security notification, auth-view white "Continue with NINAuth" button per brand rules | LIVE approval UX in the real NINAuth app; binding-email stand-in replaced by real app approval |
| Biometric binding / identity assurance | DG "Device and browser requirements" + "Authenticate with NINAuth" | **DOCUMENTED** | n/a (app onboarding) | n/a | n/a | n/a | n/a | Onboarding = live capture + liveness check + match against NIMC NIN-record photo; one NIN per app instance; offline device-biometric unlock; front camera required | Our mock honestly labels the binding-email field as the stand-in for the biometrically-bound identity (disappears in LIVE); we never claim per-transaction biometrics | Whether LIVE assertions expose assurance-level claims |
| Branding / button rules | DG "NINAuth button guidelines" + "Logo and brand use" | **DOCUMENTED** | n/a | n/a | n/a | n/a | n/a | White #FFFFFF (default) or green #008643; labels "Continue/Sign in/Verify/Authenticate with NINAuth"; one-word NINAuth; aria-label 'NIN Auth'; ≥44px touch; no endorsement implication; official asset pack | auth-view flagship button is white-filled with emerald accents, NINAuth brand treatment; email demoted to outline; "integration is not endorsement" respected in copy | Hover typo on official page (#D1FAE5G, presumed #D1FAE5); dark-mode button asset specifics |
| Approved-partner verification (public) | SITE /approved-partners | **DOCUMENTED** | ninauth.nimc.gov.ng/approved-partners (public search UI) | search by name | — | n/a | Lets citizens verify requester before consenting | n/a | Not integrated; candidate for LIVE "verify the verifier" UX | Backing API (if any) for programmatic checks |
| Operational status (public) | SITE /status | **DOCUMENTED** | ninauth.nimc.gov.ng/status | — | components: QRcode Auth Service, Sharecode Verification, Biometric Verification, ID Check Services, Email/OTP-SMS Authentication | n/a | n/a | n/a | Provider-health vocabulary reserved for LIVE degraded-posture UX | Machine-readable status feed (RSS/JSON?) |
| Governance / standards claims | SITE /about-nin-auth, /standards-adopted, /privacy-and-security | **DOCUMENTED** | n/a | n/a | n/a | n/a | NDPA consent-based access; law-enforcement disclosure carve-out; deletion possible but discouraged | PKI/ECC (NPKI w/ NITDA), NTP, permissioned DLT (metadata only, no PII), OSIA, OAuth 2.0/OIDC/WebAuthn/FIDO UAF, DID v1.0/VC v1.1/DIDComm v2/DWN | Our policy explainer + DPoA posture aligns; DLT/OSIA claims are NIMC-side, not ours to inherit | Third-party audit cadence; SLA numbers (none published) |

**How to read this matrix when LIVE credentials arrive:** every UNCONFIRMED cell is a mandatory sandbox-verification checklist item; every PARTIAL cell needs its missing URL/field/format confirmed before code depends on it; DOCUMENTED cells are safe to build against as-is (modulo NIMC versioning of the Postman collection, currently v1.0.1).
